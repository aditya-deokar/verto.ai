"use client";

import RenderPage from "./RenderPage";

export function CreatePageClient() {
    // Go directly to PPT creation options - mobile design now has its own route
    return <RenderPage />;
}

